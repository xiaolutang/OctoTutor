# Evidence: R017

## Execution Summary
- mode: direct
- status: completed
- timestamp: 2026-06-04

## Changes Made

### R017-BF001: docker-compose.yml — Traefik 路由去域名去 TLS
- 前端 labels: `Host(...)` → `PathPrefix(/)`，`websecure` → `web`，删除 tls + certresolver
- 后端 labels: `Host(...) && PathPrefix(/api/)` → `PathPrefix(/api/)`，`websecure` → `web`，删除 tls + certresolver
- 文件头部注释更新为 IP 直连模式说明
- 补充 HTTPS 自签名路由（`*-secure` routers, `websecure` entrypoint, `tls=true`）
- 后端新增 `auth-network`（PostgreSQL 访问）和 `new-api-net`（LLM 调用）网络

### R017-BF002: .remote.env — AUTH_BASE_URL 更新
- `AUTH_BASE_URL=https://auth.xiaolutang.top` → `https://111.229.125.161/auth`（HTTPS，避免混合内容）

### Simplify 热修
- `NEWAPI_BASE_URL=http://new-api:13000/v1` → `http://new-api:3000/v1`（Docker 内部网络用容器端口而非宿主机映射端口）
- `deploy/.remote.env.example` + `deploy/setup-env.sh`: AUTH_BASE_URL 模板改为 `https://your-server-ip/auth`

## Acceptance Criteria Verification

| 任务 | 条件 | 结果 |
|------|------|------|
| BF001 | 前端 labels 无 Host、无 certresolver，HTTP entrypoints=web | PASS |
| BF001 | 后端 labels 无 Host、无 certresolver，HTTP entrypoints=web | PASS |
| BF001 | 文件注释更新为 IP 直连模式说明 | PASS |
| BF001 | HTTPS 自签名路由补充（`*-secure` routers） | PASS |
| BF002 | AUTH_BASE_URL 值为 https://111.229.125.161/auth | PASS |
| simplify | NEWAPI_BASE_URL 端口修正 13000→3000 | PASS |
| simplify | 模板 AUTH_BASE_URL 统一为 https:// | PASS |

## Deployment Verification (已验证)

1. ✅ `https://111.229.125.161` 可访问前端页面
2. ✅ `/api/config` 返回 `{"authCenterBaseURL":"https://111.229.125.161/auth"}`
3. ✅ 飞书 OAuth 登录成功
4. ✅ SSE 聊天流式输出正常

## Out-of-Scope Items (需手动完成)
- auth-center Traefik labels 改为 PathPrefix(/auth/) + StripPrefix(/auth)
- auth-center 支持 root_path="/auth"
- auth-center 管理后台添加回调地址 http://111.229.125.161/callback
