# Sprint Contract: R009-BB001

## 状态
agreed

## 源文件
`backend/app/chat/conversation_router.py`

## 依赖文件
- `backend/app/chat/errors.py` — ConversationErrorCode + make_conversation_error
- `backend/app/chat/schemas.py` — ConversationListResponse / ConversationItemResponse / ConversationUpdateRequest
- `backend/app/infra/conversation_repo.py` — ConversationRepo (list_by_user / get_by_id / update / delete_by_id / count_pinned)
- `backend/app/domain/models.py` — Conversation ORM

## Hard Gates (必须全部通过)

| # | 验收条件 | 验证方法 | 代码位置 | 状态 |
|---|---------|---------|---------|------|
| HG1 | GET /api/conversations 返回分页列表（items + cursor + has_more） | 调用 GET /api/conversations，验证响应体包含 items 数组、cursor 字段、has_more 布尔值 | `list_conversations` L257-275 | PASS |
| HG2 | PATCH /api/conversations/{id} 重命名成功返回 200 + 更新后的对话数据 | PATCH 请求 body.title="新标题"，验证 200 响应包含 title 字段已更新 | `update_conversation` L278-313 | PASS |
| HG3 | PATCH /api/conversations/{id} 置顶成功，pinned=true + pinned_at 有值 | PATCH 请求 body.pinned=true，验证响应 pinned=true 且 pinned_at 非空 | L298-304 | PASS |
| HG4 | PATCH /api/conversations/{id} 置顶超限（>=5）返回 400 + 错误码 03902 | 已有 5 条置顶时发送 PATCH pinned=true，验证 400 + code="03902" | L300-302, errors.py PIN_LIMIT="03902" | PASS |
| HG5 | PATCH /api/conversations/{id} 标题为空或超长(>200)返回 400 + 错误码 03903 | 发送 title="" 或 201 字符串，验证 400 + code="03903" | L294, errors.py TITLE_INVALID="03903" | PASS |
| HG6 | DELETE /api/conversations/{id} 删除成功返回 204 | 删除已存在对话，验证 204 No Content | L338 | PASS |
| HG7 | DELETE /api/conversations/{id} 不存在返回 404 | 删除不存在 ID，验证 404 + code="03901" | L327 | PASS |
| HG8 | 所有端点验证 user_id 归属 | 使用不同用户访问他人对话，验证全部返回 404 或空列表 | get_current_user 依赖 + ConversationRepo 所有查询都带 user_id 条件 | PASS |
| HG9 | 现有 GET /api/conversations/current 不受影响 | 调用 GET /api/conversations/current，验证行为与修改前一致 | L37-69，代码未变更 | PASS |

## Scored Criteria (评分项)

| # | 评分维度 | 权重 | 达标标准 | 实现说明 |
|---|---------|------|---------|---------|
| SC1 | 分页游标正确性 | 15% | 游标格式为 base64("{updated_at_iso}\|{id}")，翻页返回的 items 全部在 cursor 之前 | L270-273 生成游标；ConversationRepo.list_by_user L74-95 解码游标并用 updated_at+id 联合排序 |
| SC2 | 置顶与普通混合排序 | 15% | 首页返回全部置顶（按 pinned_at DESC）+ 普通对话（按 updated_at DESC），翻页只返回普通 | ConversationRepo.list_by_user L51-72 首页双查询合并 |
| SC3 | 错误响应格式一致性 | 10% | 所有错误返回 {"code": string, "message": string, "action": string} | make_conversation_error 统一生成，CONVERSATION_ERROR_REGISTRY 定义完整 |
| SC4 | 并发安全 | 10% | 置顶计数查询与更新之间不出现超限竞态 | 当前实现为查询-判断-更新非原子操作，高并发下可能超限。风险已知，当前用户规模可接受 |
| SC5 | checkpoint 清理健壮性 | 10% | DELETE 删除对话后尝试清理 checkpoint，清理失败不阻断响应 | L331-336 try/except 包裹，失败只 log warning |
| SC6 | limit 参数边界 | 10% | limit 范围 [1, 50]，默认 20 | L259 Query(20, ge=1, le=50) |
| SC7 | 标题 trim 处理 | 5% | 空白标题（纯空格）被拒绝，有效标题去除首尾空格后存储 | L294 `not body.title.strip()` 校验 + L296 `body.title.strip()` 存储 |
| SC8 | PATCH 无有效更新字段时的行为 | 5% | body 中 title 和 pinned 都为 None 时，直接返回当前数据不报错 | L309 `if updates:` 判断，无更新跳过数据库操作 |
| SC9 | 取消置顶清空 pinned_at | 10% | pinned=false 时 pinned_at 置为 None | L305-307 `updates["pinned"] = False; updates["pinned_at"] = None` |
| SC10 | _conv_to_response 映射完整性 | 10% | ORM Conversation 所有字段正确映射到 ConversationItemResponse | L244-254 映射 id/title/pinned/pinned_at/message_count/created_at/updated_at |

## 测试要求
- 单元测试: ConversationRepo 各方法（create/list_by_user/update/delete_by_id/count_pinned）使用 mock AsyncSession
- 集成测试: 使用 TestClient + 测试数据库覆盖全部 9 条 HG
- 边界测试: limit=1/50、cursor 解码异常、超长标题(201字符)、纯空格标题、不存在的 conversation_id

## 备注
- PATCH 成功时返回的是 ConversationItemResponse，FastAPI 默认 status_code=200，无需显式设置
- ConversationRepo.list_by_user 首页策略：先查全部 pinned（最多5条），再查 normal 补齐 limit，可能导致首页返回超过 limit 条（pinned 全部 + normal 补齐）
- AC4 的置顶上限硬编码为 5（L301），后续可考虑提取为配置
