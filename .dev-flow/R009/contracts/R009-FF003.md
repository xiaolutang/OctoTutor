# Sprint Contract: R009-FF003

## 状态
agreed

## 源文件
- `frontend/src/contexts/conversation-context.tsx`
- `frontend/src/chat/use-conversation-list.ts`

## Hard Gates
| # | 验收条件 | 验证方法 | 状态 |
|---|---------|---------|------|
| 1 | ConversationProvider 包裹后，子组件可通过 useConversationContext() 获取状态 | `ConversationProvider` (L168) 创建 Context，`useConversationContext` (L156) 通过 useContext 消费，无 Provider 时抛错 | PASS |
| 2 | useReducer 管理 items/activeId/isNewConversation/cursor/hasMore/isLoading/isInitialized | `initialState` (L64-72) 包含全部 7 个字段，`useReducer(conversationReducer, ...)` (L169) | PASS |
| 3 | 初始化时 GET /api/conversations?limit=20 加载列表 | `useEffect` (L177-200) 调用 `fetchConversationList(undefined, 20)` (L182)，底层 `fetchWithAuth('/conversations?limit=20')` (use-conversation-list.ts L17) | PASS |
| 4 | activeId 存储到 sessionStorage，页面刷新后可恢复 | `getStoredActiveId` (L42-49) 从 sessionStorage 读取，`storeActiveId` (L52-61) 写入，初始化 state 时 L171 调用 `getStoredActiveId()` | PASS |
| 5 | switchTo(id) 切换 activeId 并更新 sessionStorage | `switchTo` (L202-204) dispatch SET_ACTIVE，reducer L91 调用 `storeActiveId(action.payload)` 同步写入 sessionStorage | PASS |
| 6 | createNew() 设置 activeId=null + isNewConversation=true | `createNew` (L206-208) dispatch SET_NEW_CONVERSATION(true)，reducer L94 设 `{ isNewConversation: true, activeId: null }` | PASS |
| 7 | insertNewConversation(item) 在列表顶部插入新对话 | `insertNewConversation` (L210-212) dispatch INSERT_NEW，reducer L96-101 `[action.payload, ...state.items]` + 设 activeId + isNewConversation=false | PASS |
| 8 | updateTitle(id, title) 更新列表中指定对话的标题 | `updateTitle` (L214-216) dispatch UPDATE_TITLE，reducer L103-110 map 找到匹配 id 更新 title | PASS |
| 9 | loadMore() 加载下一页对话 | `loadMore` (L218-229) 检查 hasMore/cursor 后调用 `fetchConversationList(state.cursor, 20)`，dispatch APPEND_PAGE 追加分页数据 | PASS |
| 10 | removeConversation(id) 从列表移除 | `removeConversation` (L231-233) dispatch REMOVE_ITEM，reducer L118-122 filter 移除指定 id | PASS |
| 11 | renameConversation(id, title) 调用 PATCH API | `renameConversation` (L235-241) 调用 `patchConversation(id, { title })`，底层 `fetchWithAuth('/conversations/${id}', { method: 'PATCH' })` (use-conversation-list.ts L29) | PASS |
| 12 | pinConversation(id) 调用 PATCH API | `pinConversation` (L243-251) 调用 `patchConversation(id, { pinned: true })` | PASS |
| 13 | unpinConversation(id) 调用 PATCH API | `unpinConversation` (L253-260) 调用 `patchConversation(id, { pinned: false })` | PASS |
| 14 | deleteConversation(id) 删除当前 activeId 对话后，自动设置 activeId 为列表第一个对话或 null | `deleteConversation` (L262-276) 先调 `deleteConversationApi`，再 REMOVE_ITEM，L267-273 判断若 activeId===id 则切到 `remaining[0].id` 或 `null` | PASS |

## Scored Criteria
| # | 评分维度 | 权重 | 达标标准 |
|---|---------|------|---------|
| S1 | 初始化健壮性 | 20% | useEffect 内有 cancelled 标志防止卸载后更新；初始化列表后检查 activeId 是否在列表中，不在则清除 (L186-188) |
| S2 | 并发安全 | 15% | loadMore 使用 loadingMoreRef 防止重复请求 (L174, L220)；deleteConversation 基于 state.activeId/state.items 最新值计算后续状态 (L267-273) |
| S3 | sessionStorage 容错 | 10% | getStoredActiveId (L42-49) 捕获异常、过滤 undefined/null 字符串；storeActiveId (L52-61) 捕获异常静默处理 |
| S4 | API 层职责分离 | 15% | use-conversation-list.ts 负责纯 API 调用 (fetch/patch/delete)，conversation-context.tsx 负责 UI 状态管理，职责清晰 |
| S5 | 类型安全 | 10% | ConversationAction 联合类型 (L23-38) 精确描述所有 action；ConversationContextValue 接口 (L139-152) 完整暴露方法和状态 |
| S6 | Reducer 纯度 | 10% | SET_ACTIVE (L91) 中调用 storeActiveId 是唯一副作用——属于 sessionStorage 同步的必要设计，整体可接受 |
| S7 | 删除后状态一致性 | 10% | 删除 activeId 对话后自动切换到 remaining[0] 或 null (L267-273)，SET_ACTIVE 同时更新 sessionStorage + 重置 isNewConversation |
| S8 | 错误处理 | 10% | 初始化失败时恢复 isLoading=false (L192)；pinConversation/unpinConversation 捕获异常并 rethrow 明确错误信息供调用方 toast |
