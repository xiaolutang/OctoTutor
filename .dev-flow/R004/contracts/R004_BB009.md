# Sprint Contract: R004_BB009

## Metadata
- task_id: BB009
- status: agreed
- domain: integration
- created_at: 2026-05-21

## Implementation Plan

### 1. POST /api/chat 端到端测试
- 使用 FastAPI TestClient 测试 POST /api/chat
- Mock 底层依赖（vector_store, embedding, reranker, generator, bm25）
- 验证 answer 非空 + sources 非空 + context_used > 0
- 测试正常路径和 degraded 路径

### 2. Rerank 降级集成测试
- Mock reranker 抛出异常
- 验证 ChatService 返回 degraded=true + degradation_reason
- 回答仍正常生成

### 3. Context Precision 评估集成测试
- 使用 EvalRunner.run_context_precision() with mock services
- 验证输出 Precision@K 数值 (0-1)

### 4. Regression 模式集成测试
- 使用 EvalRunner.run_regression() with mock services
- 验证输出 EvalReport 格式正确
- 注意：真实 Span Hit@5 ≥ 97% 需要真实服务，此处只验证模式可运行

### 5. Faithfulness 评估集成测试
- 使用 EvalRunner.run_faithfulness() with mock services
- 验证输出 FaithfulnessReport 格式正确
- 注意：真实评估需要标注数据，此处只验证管线可运行

## Acceptance Criteria
1. POST /api/chat TestClient 测试通过（正常 + degraded 路径）
2. Rerank 降级时 degraded=true，回答仍正常
3. run_context_precision 输出有效 Precision@K
4. run_regression 输出 EvalReport
5. run_faithfulness 输出 FaithfulnessReport（需 reranker+generator）
6. 单元测试全部通过
