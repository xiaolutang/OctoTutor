# Sprint Contract: R004_BB006

## Metadata
- task_id: BB006
- status: agreed
- domain: evaluation
- created_at: 2026-05-21

## Implementation Plan

### 1. EvalItem 扩展 (evaluation/eval_types.py)
- 新增字段 `key_facts: list[str] = field(default_factory=list)` — 关键知识点列表
- 新增字段 `reference_answer: str = ""` — 参考答案
- 新增字段 `suite: str = "regression"` — 用例归属标记
- 更新 `to_dict()` — 序列化新字段（key_facts 非空时输出、reference_answer 非空时输出、suite 非 "regression" 时输出）
- 更新 `from_dict()` — 反序列化新字段，缺失时使用默认值
- 向后兼容：现有代码构造 EvalItem 不带新字段时默认值正确

### 2. DeterministicGrader (evaluation/graders/deterministic.py)
- 新建 `evaluation/graders/__init__.py`
- 新建 `evaluation/graders/deterministic.py`
- `GradingResult` dataclass: passed (bool), failures (list[str])
- `DeterministicGrader` class with `check(answer, sources, context)` method:
  - answer 非空: `len(answer.strip()) > 0`
  - sources 非空: `len(sources) > 0`
  - 引用页码在 context 范围内: sources 的 page_start/page_end 与 context chunks 的 page 范围有交集
  - 无重复 chunk_id: sources 中 chunk_id 唯一
- 签名: `check(answer: str, sources: list[SourceReference], context: list[QueryResult]) -> GradingResult`
- 任一检查不通过 → passed=False, failures 列出原因
- 全部通过 → passed=True, failures=[]

### 3. 单元测试
- EvalItem 向后兼容测试（不带新字段构造、to_dict/from_dict 往返）
- DeterministicGrader 全场景测试（空 answer、空 sources、页码超范围、重复 chunk_id、全部通过）

## Acceptance Criteria (Negotiated)
1. EvalItem 新增 3 个字段均有默认值，现有代码零改动即可运行
2. EvalItem.to_dict() / from_dict() 往返一致性
3. DeterministicGrader.check() 正确判定 PASS/FAIL
4. 确定性检查 4 项覆盖：answer 非空、sources 非空、页码在范围内、无重复 chunk_id
5. 单元测试全部通过
