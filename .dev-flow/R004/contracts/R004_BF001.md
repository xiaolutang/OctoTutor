# Sprint Contract: R004_BF001

## Metadata
- task_id: BF001
- rc_id: R004
- status: agreed
- created_at: 2026-05-21

## Domain
domain — 共享数据模型 + Protocol 接口定义

## Implementation Plan
1. 创建 `backend/app/domain/__init__.py`（空包）
2. 创建 `backend/app/domain/models.py`（SourceReference Pydantic BaseModel，5 个字段）
3. 创建 `backend/app/domain/protocols.py`（Reranker + Generator Protocol 接口）

## Acceptance Criteria (Negotiated)
1. SourceReference 包含 chunk_id, book, section, page_start, page_end 字段
2. Reranker Protocol 含 rerank(query, results, top_n) → list[QueryResult]
3. Generator Protocol 含 generate(query, context_chunks) → tuple[str, list[SourceReference]]
4. 全链路 import 无报错
5. Protocol 使用 typing.Protocol（非 ABC）
6. SourceReference 字段名与 ChunkMetadata 一致（book, section, page_start, page_end）

## Constraints
- 不修改 rag/models.py
- 不修改 config.py
- 不修改 main.py
- SourceReference 使用 Pydantic BaseModel
