# Sprint Contract: R005_BF001

## Metadata
- task_id: BF001
- status: agreed
- domain: chat — SSE 事件模型 + 错误码体系 + RetrieveResult
- created_at: 2026-05-22
- depends_on: []
- priority: 1
- verification: L2
- analysis_ref: 2026-05-21--R005-chat-ui-sse
- decision_refs: [DEC-chatui-025, DEC-chatui-030]
- capability_refs: [CAP-chatui-001, CAP-chatui-003]

## Implementation Plan

### Step 1. schemas.py 新增 StreamEvent + StatusPayload

**文件**: `backend/app/chat/schemas.py`

在现有 ChatRequest / ChatResponse 之后，新增两个 dataclass：

```python
from dataclasses import dataclass
from typing import Any, Literal

@dataclass
class StreamEvent:
    """SSE 流式事件封装"""
    type: Literal["status", "sources", "token", "done", "error"]
    data: Any

@dataclass
class StatusPayload:
    """status 事件的载荷"""
    stage: Literal["retrieving", "generating"]
    message: str
```

**要点**：
- 使用 `dataclasses.dataclass`（非 Pydantic），与 ChatRequest/ChatResponse 的 Pydantic BaseModel 区分
- StreamEvent.type 使用 `Literal` 约束为 5 种事件类型
- StatusPayload.stage 使用 `Literal` 约束为 2 种阶段
- 新增 import 行（dataclasses, typing）不与现有 pydantic import 冲突

### Step 2. 新建 errors.py — ChatErrorCode + ErrorDef + ERROR_REGISTRY + make_error()

**文件**: `backend/app/chat/errors.py`（新建）

```python
from dataclasses import dataclass
from enum import Enum

class ChatErrorCode(Enum):
    """对话模块错误码 — MMPPN 编码，字符串值避免前导零问题"""
    EMBEDDING_FAILED = "02102"
    VECTOR_STORE_ERROR = "02103"
    LLM_CONNECT_FAILED = "02201"
    LLM_STREAM_ERROR = "02202"
    LLM_EMPTY_RESPONSE = "02203"
    LLM_TIMEOUT = "02204"
    LLM_RATE_LIMITED = "02205"
    INTERNAL_ERROR = "02901"

@dataclass(frozen=True)
class ErrorDef:
    code: str
    message: str
    action: str  # "retry" | "edit" | "wait"

ERROR_REGISTRY: dict[ChatErrorCode, ErrorDef] = {
    ChatErrorCode.EMBEDDING_FAILED: ErrorDef("02102", "检索服务异常，请重试", "retry"),
    ChatErrorCode.VECTOR_STORE_ERROR: ErrorDef("02103", "检索服务异常，请重试", "retry"),
    ChatErrorCode.LLM_CONNECT_FAILED: ErrorDef("02201", "AI 服务连接失败，请重试", "retry"),
    ChatErrorCode.LLM_STREAM_ERROR: ErrorDef("02202", "生成中断，已保留部分内容", "retry"),
    ChatErrorCode.LLM_EMPTY_RESPONSE: ErrorDef("02203", "AI 未能生成回答，请重试", "retry"),
    ChatErrorCode.LLM_TIMEOUT: ErrorDef("02204", "AI 响应超时，请重试", "retry"),
    ChatErrorCode.LLM_RATE_LIMITED: ErrorDef("02205", "请求太频繁，请稍后再试", "wait"),
    ChatErrorCode.INTERNAL_ERROR: ErrorDef("02901", "服务异常，请重试", "retry"),
}

def make_error(code: ChatErrorCode) -> dict:
    defn = ERROR_REGISTRY[code]
    return {"code": defn.code, "message": defn.message, "action": defn.action}
```

**要点**：
- ChatErrorCode 枚举值使用**字符串**（非整数），避免 `02102` 前导零丢失问题
- ErrorDef 使用 `frozen=True` 确保不可变
- ERROR_REGISTRY 为模块级常量，每个枚举值对应一条 ErrorDef
- make_error() 返回 plain dict，方便 JSON 序列化
- 错误码 MMPPN 编码规则：`02` = 对话模块，`1` = 检索，`2` = LLM，`9` = 内部

### Step 3. service.py 新增 RetrieveResult dataclass

**文件**: `backend/app/chat/service.py`

在 ChatService 类定义之前，新增 RetrieveResult dataclass：

```python
from dataclasses import dataclass

@dataclass
class RetrieveResult:
    """_retrieve() 返回类型：携带检索结果 + 降级状态"""
    chunks: list[QueryResult]
    degraded: bool = False
    degradation_reason: str | None = None
```

**要点**：
- 位置：service.py 顶部 import 区之后、ChatService 类定义之前
- chunks 类型为 `list[QueryResult]`，复用 RAG 层现有类型
- degraded 默认 False，degradation_reason 默认 None
- 此类型为 BB002 `_retrieve()` 方法的返回值做准备，本步骤不实现 _retrieve()

### Step 4. 单元测试

**文件**: `backend/tests/test_chat_types.py`（新建）

测试覆盖 3 个维度：

1. **ChatErrorCode 枚举值验证**：确认 8 个枚举成员的值均为 5 位字符串
2. **make_error() 返回格式验证**：每个枚举值调用 make_error() 返回 dict 包含 code/message/action 三个 key；特别验证 `make_error(ChatErrorCode.EMBEDDING_FAILED)` 返回 `{"code": "02102", "message": "检索服务异常，请重试", "action": "retry"}`
3. **RetrieveResult 构造和默认值**：验证默认 degraded=False, degradation_reason=None；验证带参构造正确赋值

## Acceptance Criteria (Negotiated)

1. **schemas.py 扩展**：StreamEvent 和 StatusPayload 可从 `app.chat.schemas` import；StreamEvent.type 支持 5 种字面量，StatusPayload.stage 支持 2 种字面量
2. **errors.py 新建**：ChatErrorCode 包含 8 个枚举成员，值均为 5 位字符串；ERROR_REGISTRY 覆盖全部 8 个枚举值
3. **make_error() 契约**：`make_error(ChatErrorCode.EMBEDDING_FAILED)` 返回 `{"code": "02102", "message": "检索服务异常，请重试", "action": "retry"}` 格式的 dict
4. **RetrieveResult**：可从 `app.chat.service` import；支持无参默认构造（chunks 必传）和完整构造
5. **全量 import 无报错**：`from app.chat.schemas import StreamEvent, StatusPayload`、`from app.chat.errors import ChatErrorCode, ErrorDef, make_error, ERROR_REGISTRY`、`from app.chat.service import RetrieveResult` 均可成功 import
6. **现有代码不受影响**：ChatRequest / ChatResponse / ChatService 功能不变
7. **单元测试通过**：3 组单元测试全部通过

## Negotiated Risks

| ID | Risk | Mitigation |
|----|------|-----------|
| RSK-BF001-01 | 枚举值字符串与前端硬编码不一致 | 前端使用 code 字符串匹配，后端枚举值仅后端内部使用，不直接暴露给前端 |
