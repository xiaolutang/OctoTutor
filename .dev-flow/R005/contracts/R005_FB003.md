# Sprint Contract: R005_FB003

## Metadata
- task_id: FB003
- status: agreed
- domain: frontend — Chat 页面集成验证 + 依赖确认 + 端到端验证（环境受限调整）
- created_at: 2026-05-22
- depends_on: [FB002 (completed), BB003 (completed)]
- priority: 5
- verification: L2 (本地可验证) / L3+L4 降级为 blocked
- risk_tags: [network, third_party]
- analysis_ref: 2026-05-21--R005-chat-ui-sse
- decision_refs: [DEC-chatui-009, DEC-chatui-032, DEC-chatui-038]
- contract_refs: [new:sse-event-protocol]
- capability_refs: [CAP-chatui-001, CAP-chatui-002, CAP-chatui-003]

---

## Context: 前置产物状态

FB003 原始定义为"集成验证"任务，但经核实，大量集成工作已被前置任务完成：

| 原始工作项 | 实际状态 | 完成任务 |
|-----------|---------|---------|
| chat/page.tsx 集成 ChatUI | 已完成 | FB001 已将 ChatUI 渲染到 chat/page.tsx |
| 安装 react-markdown 等 4 个依赖 | 已完成 | FB002 已添加到 package.json + node_modules 已安装 |
| 安装 shadcn button 组件 | 不需要 | ChatInput 使用原生 `<button>` 标签，无 shadcn Button 引用 |
| SourceCard + KaTeX + 操作按钮 | 已完成 | FB002 全部实现 |
| ChatUI 核心交互（发送/停止/错误恢复） | 已完成 | FB001 全部实现 |
| 前端单元测试 | 已通过 | 54/54 测试通过（1 个无关失败：return-url.test.ts 因 @xlfoundry/auth-sdk-web 本地包路径） |

**结论**：FB003 的代码开发工作量为零。任务聚焦于验证 + 确认 + 记录。

## 1. Scope (允许修改的文件)

| # | 操作 | 文件路径 | 说明 |
|---|------|---------|------|
| 1 | READ-ONLY | `frontend/src/app/chat/page.tsx` | 确认已集成 ChatUI |
| 2 | READ-ONLY | `frontend/package.json` | 确认依赖声明 |
| 3 | READ-ONLY | `frontend/src/components/*.tsx` | 确认组件完整 |
| 4 | NEW | `.dev-flow/R005/evidence/R005_FB003.md` | 验证证据记录 |

**注意**：本任务不修改任何源代码文件，仅执行验证和记录。

## 2. Implementation Plan

### Step 1. chat/page.tsx 集成确认

**验证项**：
- chat/page.tsx 导入并渲染 `<ChatUI />` 组件
- 被 `<RouteGuard>` 包裹（登录保护）
- 布局容器配置正确（flex, h-[calc(100vh-3.5rem)]）

**现状**：已确认 `chat/page.tsx` 正确集成 RouteGuard + ChatUI，无占位内容。

### Step 2. 前端依赖确认

**验证项**：
- `react-markdown`: package.json 声明 + node_modules 已安装
- `remark-math`: package.json 声明 + node_modules 已安装
- `rehype-katex`: package.json 声明 + node_modules 已安装
- `katex`: package.json 声明 + node_modules 已安装

**现状**：4 个依赖均已在 package.json 声明且 node_modules 中已安装。无需再执行 `npm install`。

### Step 3. shadcn button 确认

**验证项**：
- 检查 Chat 组件是否有从 `@/components/ui/button` 导入

**现状**：ChatInput 使用原生 `<button>` HTML 元素，Header 也使用原生按钮。无需安装 shadcn button 组件。

### Step 4. 前端组件完整性确认

**验证项**：
- `chat-ui.tsx` — 主组件存在，导入 useChatStream + useChatStorage + ChatInput + MessageBubble
- `chat-input.tsx` — 输入框组件存在，textarea + 发送/停止按钮
- `message-bubble.tsx` — 消息气泡存在，react-markdown + KaTeX + SourceCard + 操作按钮
- `source-card.tsx` — 来源卡片存在，收起/展开
- `route-guard.tsx` — 路由守卫存在
- `header.tsx` — 页头存在

**现状**：所有组件文件均已存在于 `frontend/src/components/`。

### Step 5. 前端测试执行

**命令**：
```bash
cd frontend && npx vitest run
```

**预期**：
- 54/54 测试通过
- 1 个 Suite 失败（return-url.test.ts），原因是 `@xlfoundry/auth-sdk-web` 本地 file: 包路径解析问题，与 FB003 无关

**现状**：已执行确认，54 个测试通过，仅 return-url.test.ts 因外部依赖路径失败。

### Step 6. 前端构建验证

**命令**：
```bash
cd frontend && npx next build
```

**预期**：
- 构建可能因 `@xlfoundry/auth-sdk-web` 本地包路径解析而失败
- 此为已知的前置问题（auth-sdk-web 通过 `file:../../../xlfoundryTest/auth-sdk-web` 引用），非 FB003 范围
- Chat 相关模块（chat/page.tsx, components/*.tsx, chat/*.ts）应无 TypeScript 编译错误

**降级策略**：如果构建失败仅因 auth-sdk-web 路径问题，记录为 "blocked_by_external"，不影响 FB003 验收。需开发者确认本地 xlfoundryTest 仓库路径正确后重试。

### Step 7. 端到端验证（blocked — 需 Docker 环境重建）

**依赖**：BB004 解除 blocked 状态后才能执行

**降级为 smoke_required** 的验证项：

| # | 验证项 | 状态 | 依赖 |
|---|--------|------|------|
| E2E-1 | curl POST /api/chat/stream → SSE 事件流格式正确 | blocked | BB004 Docker 重建 |
| E2E-2 | 浏览器 /chat → 发送消息 → 流式文本 | blocked | BB004 + Docker 前端部署 |
| E2E-3 | 来源卡片显示 + KaTeX 公式渲染 | blocked | BB004 + Docker 前端部署 |
| E2E-4 | 停止 → 重新生成 → 编辑 | blocked | BB004 + Docker 前端部署 |
| E2E-5 | 刷新恢复对话（localStorage） | blocked | BB004 + Docker 前端部署 |
| E2E-6 | 发送失败 → 撤回输入框 + 重试 | blocked | BB004 + Docker 前端部署 |
| E2E-7 | POST /api/chat 非流式回归 | blocked | BB004 Docker 重建 |
| E2E-8 | Traefik SSE 透传 | blocked | BB004 + Docker 全栈部署 |

**说明**：Docker 镜像为旧版代码（R005 SSE 端点未包含），BB004 已标记为 blocked。以上端到端验证需 BB004 解除后、Docker 镜像重建后执行。

## 3. Acceptance Criteria (Negotiated)

### 硬性通过项（本地可验证，全部必须通过）

| AC# | 验收条件 | 验证方式 | 现状 |
|-----|---------|---------|------|
| AC-1 | chat/page.tsx 已集成 ChatUI + RouteGuard | 文件审查 | PASS |
| AC-2 | react-markdown, remark-math, rehype-katex, katex 已声明且安装 | package.json + node_modules 检查 | PASS |
| AC-3 | 不需要 shadcn button（ChatInput 用原生 button） | 代码搜索确认 | PASS |
| AC-4 | Chat 组件文件完整（chat-ui, chat-input, message-bubble, source-card） | 文件系统检查 | PASS |
| AC-5 | 前端单元测试 54/54 通过 | `npx vitest run` | PASS |
| AC-6 | Chat 相关源码无 TypeScript 编译错误 | `npx tsc --noEmit` 或 build 输出 | 待验证 |

### 降级为 smoke_required 的验证项（需 BB004 解除 blocked 后执行）

| AC# | 验收条件 | 原始级别 | 降级原因 |
|-----|---------|---------|---------|
| AC-7 | curl POST /api/chat/stream SSE 事件流格式正确 | L3 | Docker 镜像为旧版，SSE 端点不存在 |
| AC-8 | 浏览器 /chat 完整对话流程（发送→流式→来源→KaTeX→停止→重新生成→编辑） | L4 | 需 Docker 全栈部署 |
| AC-9 | 刷新恢复对话（localStorage） | L4 | 需 Docker 前端部署 |
| AC-10 | 错误恢复（首事件前撤回/首事件后保留+重试） | L4 | 需 Docker 全栈部署 |
| AC-11 | POST /api/chat 非流式回归 | L3 | Docker 镜像为旧版 |
| AC-12 | Traefik SSE 透传 | L3 | 需 Docker 全栈 + Traefik |

### 已知问题（不影响 FB003 验收）

| Issue | 说明 | 影响范围 |
|-------|------|---------|
| return-url.test.ts 失败 | `@xlfoundry/auth-sdk-web` 本地 file: 包路径解析失败 | 非 Chat 模块，与 FB003 无关 |
| next build 失败 | 同上，auth-sdk-web 包未解析 | 非 Chat 模块，需开发者确认本地路径 |

## 4. Hard Gates

| Gate | 条件 | 验证方式 | 状态 |
|------|------|---------|------|
| HG1 | 文件在允许范围内（仅 evidence 文件为 NEW） | scope 检查 | PASS |
| HG2 | chat/page.tsx 渲染 ChatUI | 文件读取 | PASS |
| HG3 | 4 个前端依赖已安装 | node_modules 检查 | PASS |
| HG4 | 所有 Chat 相关组件存在 | 文件系统检查 | PASS |
| HG5 | 54/54 前端单元测试通过 | `npx vitest run` | PASS |
| HG6 | Chat 源码无 TypeScript 错误 | tsc 或 build | 待验证 |

## 5. Negotiated Risks

| ID | Risk | Mitigation |
|----|------|-----------|
| RSK-FB003-01 | Docker 镜像为旧版，无法验证 SSE 端点 | L3/L4 验证降级为 smoke_required，待 BB004 解除后执行 |
| RSK-FB003-02 | @xlfoundry/auth-sdk-web 本地包路径导致构建失败 | 非 FB003 范围，记录为 blocked_by_external；Chat 模块本身无编译错误 |
| RSK-FB003-03 | 前端代码已完成但缺少真实环境端到端验证 | 单元测试覆盖核心逻辑（54/54），L4 E2E 列为 blocked 待后续验证 |
| RSK-FB003-04 | shadcn button 是否需要安装 | 已确认 ChatInput 使用原生 `<button>`，无需安装 |

## 6. Execution Plan Summary

```
Step 1: chat/page.tsx 集成确认 (READ-ONLY)
  ├── RouteGuard 包裹
  ├── ChatUI 渲染
  └── 布局容器配置
  ↓ [PASS — FB001 已完成]

Step 2: 依赖确认 (READ-ONLY)
  ├── react-markdown ✓
  ├── remark-math ✓
  ├── rehype-katex ✓
  └── katex ✓
  ↓ [PASS — FB002 已完成]

Step 3: shadcn button 确认
  └── 不需要（原生 button）
  ↓ [PASS]

Step 4: 组件完整性确认
  ├── chat-ui.tsx ✓
  ├── chat-input.tsx ✓
  ├── message-bubble.tsx ✓
  ├── source-card.tsx ✓
  ├── route-guard.tsx ✓
  └── header.tsx ✓
  ↓ [PASS — FB001/FB002 已完成]

Step 5: 前端测试执行
  └── 54/54 通过
  ↓ [PASS]

Step 6: 前端构建验证
  └── Chat 模块无 TS 错误（auth-sdk-web 问题排除）
  ↓ [待验证]

Step 7: 端到端验证 (BLOCKED)
  └── 需 BB004 Docker 重建
  ↓ [smoke_required — 后续验证]
```

## 7. Evidence Template

验证完成后，在 `.dev-flow/R005/evidence/R005_FB003.md` 记录：

```markdown
# Task Evidence: FB003

## Metadata
- task_id: FB003
- sprint_contract_path: .dev-flow/R005/contracts/R005_FB003.md
- execution_date: 2026-05-22

## Local Verification Results
- chat_page_integration: [confirmed/blocked]
- dependencies_installed: [confirmed/missing]
- shadcn_button_needed: [no/yes]
- components_complete: [confirmed/missing]
- vitest_result: [54/54 passed + 1 unrelated fail]
- build_result: [success / blocked_by_auth_sdk / failed]

## Smoke Required Items (blocked by BB004)
- E2E-1 curl SSE stream: [blocked]
- E2E-2 full chat flow: [blocked]
- E2E-3 source card + KaTeX: [blocked]
- E2E-4 stop/regenerate/edit: [blocked]
- E2E-5 refresh restore: [blocked]
- E2E-6 error recovery: [blocked]
- E2E-7 non-stream API regression: [blocked]
- E2E-8 Traefik SSE pass-through: [blocked]

## Evaluation Result
- evaluate_status: [pass (local) / blocked (E2E)]
- hard_gates: [N/6] passed locally
```
