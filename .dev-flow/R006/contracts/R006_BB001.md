---
task_id: BB001
rc_id: R006
name: "Router Depends 注入 — router.py/stream_router.py/retrieve.py"
status: draft
created: 2026-05-22
analysis_refs:
  - .dev-flow/R006/analysis/2026-05-22--R006-auth-integration.md
  - .dev-flow/R006/analysis/2026-05-22--R006-auth-integration-backend.md
architecture_ref: .dev-flow/architecture.md
decision_refs:
  - DEC-auth-002  # Depends 注入鉴权（非全局中间件），与现有 Depends 模式一致
  - DEC-auth-006  # user_id 路由层注入但 R006 不传递到 service
  - DEC-auth-007  # 不鉴权 /api/health
capability_refs:
  - CAP-auth-001  # JWT 鉴权验证
verification_level: L3
estimated_hours: 0.8
---

# Sprint Contract: BB001 — Router Depends 注入

## 1. 目标

在三个受保护端点的路由函数签名中注入 `user: UserContext = Depends(get_current_user)`，使无 token 请求被自动拦截返回 401。R006 阶段 user 参数仅注入不传递到 service 层（R007 使用 user_id 做持久化时传递）。GET /api/health 不注入，保持公开。

**本任务范围（BB001）仅包含路由层参数注入 + import 添加**。鉴权基础设施（middleware/auth.py、config.py、requirements.txt）由 BF001 提供。

## 2. 实现步骤

| Step | 操作 | 文件 | 说明 |
|------|------|------|------|
| 1 | 添加 import | `backend/app/chat/router.py` | `from app.middleware.auth import UserContext, get_current_user` |
| 2 | 注入参数 | `backend/app/chat/router.py` | `chat()` 函数签名新增 `user: UserContext = Depends(get_current_user)` |
| 3 | 添加 import | `backend/app/chat/stream_router.py` | `from app.middleware.auth import UserContext, get_current_user` |
| 4 | 注入参数 | `backend/app/chat/stream_router.py` | `stream_chat()` 函数签名新增 `user: UserContext = Depends(get_current_user)` |
| 5 | 添加 import | `backend/app/api/routes/retrieve.py` | `from app.middleware.auth import UserContext, get_current_user` |
| 6 | 注入参数 | `backend/app/api/routes/retrieve.py` | `retrieve()` 函数签名新增 `user: UserContext = Depends(get_current_user)` |
| 7 | 编写集成测试 | `backend/tests/test_auth_integration.py` | 7 个集成测试场景（见验收标准） |

## 3. 文件清单

### 修改文件

| 文件路径 | 变更内容 | 风险 |
|----------|----------|------|
| `backend/app/chat/router.py` | (1) 新增 import 行 `from app.middleware.auth import UserContext, get_current_user`；(2) `chat()` 函数签名新增参数 `user: UserContext = Depends(get_current_user)` | Low — 仅新增参数，不影响函数体 |
| `backend/app/chat/stream_router.py` | (1) 新增 import 行 `from app.middleware.auth import UserContext, get_current_user`；(2) `stream_chat()` 函数签名新增参数 `user: UserContext = Depends(get_current_user)` | Low — 仅新增参数，不影响函数体 |
| `backend/app/api/routes/retrieve.py` | (1) 新增 import 行 `from app.middleware.auth import UserContext, get_current_user`；(2) `retrieve()` 函数签名新增参数 `user: UserContext = Depends(get_current_user)` | Low — 仅新增参数，不影响函数体 |

### 新增文件

| 文件路径 | 类型 | 说明 |
|----------|------|------|
| `backend/tests/test_auth_integration.py` | 新增 | 7 个集成测试（httpx TestClient） |

### 不修改文件

| 文件路径 | 理由 |
|----------|------|
| `backend/app/api/routes/health.py` | GET /api/health 不鉴权，不声明 Depends 即不拦截 |
| `backend/app/middleware/auth.py` | BF001 范围，BB001 仅使用 |
| `backend/app/config.py` | BF001 范围 |
| `backend/app/main.py` | Depends 注入无需注册全局中间件 |
| `backend/app/chat/service.py` | R006 不传递 user_id 到 service 层（DEC-auth-006） |
| `backend/app/chat/dependencies.py` | 无需修改 |
| `backend/app/chat/schemas.py` | 无需修改 |
| `backend/app/chat/errors.py` | 无需修改 |

## 4. 技术规格

### 4.1 router.py 变更

```python
# --- 新增 import ---
from app.middleware.auth import UserContext, get_current_user

# --- 函数签名变更 ---
@router.post("/chat", response_model=ChatResponse)
async def chat(
    request: ChatRequest,
    service: ChatService = Depends(get_chat_service),
    user: UserContext = Depends(get_current_user),  # 新增鉴权注入
):
    # R006: user 注入但不传递到 service（R007 使用 user_id 做持久化时传递）
    result = service.handle_chat(request.question, request.top_k)
    if result is None:
        raise HTTPException(status_code=404, detail="未找到相关教材内容")
    return result
```

### 4.2 stream_router.py 变更

```python
# --- 新增 import ---
from app.middleware.auth import UserContext, get_current_user

# --- 函数签名变更 ---
@router.post("/chat/stream")
async def stream_chat(
    body: ChatRequest,
    http_request: Request,
    service: ChatService = Depends(get_chat_service),
    user: UserContext = Depends(get_current_user),  # 新增鉴权注入
):
    # R006: user 注入但不传递到 service
    # ... event_generator 逻辑不变 ...
```

### 4.3 retrieve.py 变更

```python
# --- 新增 import ---
from app.middleware.auth import UserContext, get_current_user

# --- 函数签名变更 ---
@router.post("/retrieve", response_model=RetrieveResponse)
async def retrieve(
    request: RetrieveRequest,
    store: ChromaDBStore = Depends(get_vector_store),
    embedding_service: DashScopeEmbedding = Depends(get_embedding_service),
    user: UserContext = Depends(get_current_user),  # 新增鉴权注入
) -> RetrieveResponse:
    # R006: user 注入但不传递到 service
    # ... 现有逻辑不变 ...
```

### 4.4 关键设计决策

- **user 参数位置**：放在函数签名最后一个参数（FastAPI Depends 参数统一放在尾部）
- **user 不传递到 service**：函数体中不使用 `user` 变量，仅靠 FastAPI 框架在调用前执行鉴权。R007 时再传递 `user.user_id` 到 service 层
- **/api/health 不动**：不声明 `Depends(get_current_user)` 即不触发鉴权，Docker 健康检查不受影响

## 5. 验收标准

### 5.1 Hard Gates（必须全部通过）

| # | Gate | 验证方法 | 通过条件 |
|---|------|----------|----------|
| H1 | 无 token POST /api/chat -> 401 | 集成测试 `test_chat_no_token_401` | status_code == 401 |
| H2 | 无 token POST /api/chat/stream -> 401 | 集成测试 `test_stream_no_token_401` | status_code == 401 |
| H3 | 无 token POST /api/retrieve -> 401 | 集成测试 `test_retrieve_no_token_401` | status_code == 401 |
| H4 | 无 token GET /api/health -> 200 | 集成测试 `test_health_no_token_200` | status_code == 200 |
| H5 | 有效 token POST /api/chat -> 200 | 集成测试 `test_chat_valid_token_200` | status_code == 200 |
| H6 | 有效 token POST /api/chat/stream -> SSE 流 | 集成测试 `test_stream_valid_token_sse` | status_code == 200, content-type == "text/event-stream" |
| H7 | 有效 token POST /api/retrieve -> 200 | 集成测试 `test_retrieve_valid_token_200` | status_code == 200 |

### 5.2 Scored Criteria

| # | Criteria | Weight | Score 0 | Score 1 | Score 2 |
|---|----------|--------|---------|---------|---------|
| S1 | 变更最小性 | 30% | 修改了函数体或 service 层 | 新增了多余代码 | 每个文件仅新增 1 行 import + 1 行参数签名 |
| S2 | 测试覆盖完整性 | 30% | 少于 4 个场景通过 | 4-6 个场景通过 | 全部 7 个场景通过 |
| S3 | 无副作用 | 25% | 改动了 health.py 或 service.py | 仅有 lint 警告 | 零修改范围外文件，零 lint 警告 |
| S4 | 测试隔离性 | 15% | 测试间有依赖 | 基本独立但有共享状态 | 每个测试独立 mock，无交叉污染 |

### 5.3 测试场景详细定义

| # | 测试名 | 端点 | Token | 预期结果 |
|---|--------|------|-------|----------|
| T1 | `test_chat_no_token_401` | POST /api/chat | 无 | status_code=401, detail 含 "Missing authentication token" |
| T2 | `test_stream_no_token_401` | POST /api/chat/stream | 无 | status_code=401, detail 含 "Missing authentication token" |
| T3 | `test_retrieve_no_token_401` | POST /api/retrieve | 无 | status_code=401, detail 含 "Missing authentication token" |
| T4 | `test_health_no_token_200` | GET /api/health | 无 | status_code=200 |
| T5 | `test_chat_valid_token_200` | POST /api/chat | 有效 JWT | status_code=200 (mock ChatService) |
| T6 | `test_stream_valid_token_sse` | POST /api/chat/stream | 有效 JWT | status_code=200, content-type="text/event-stream" (mock ChatService) |
| T7 | `test_retrieve_valid_token_200` | POST /api/retrieve | 有效 JWT | status_code=200 (mock ChromaDBStore + DashScopeEmbedding) |

### 5.4 测试辅助说明

- 使用 httpx `TestClient` + FastAPI app 实例
- 有效 JWT 通过 `_make_token()` 辅助函数构造（与 BF001 单元测试共享模式）
- ChatService / ChromaDBStore / DashScopeEmbedding 通过 `app.dependency_overrides` mock
- 无需启动真实 LLM / Embedding 服务

## 6. 依赖与约束

### 6.1 前置依赖

- **BF001**（JWT 鉴权基础）：提供 `app.middleware.auth` 模块（`get_current_user`、`UserContext`）+ config.py `auth_jwt_secret` 字段 + requirements.txt `python-jose`

### 6.2 被依赖（下游任务）

- BF003（部署配置 JWT_SECRET_KEY）：BB001 完成后端点鉴权生效，需配合部署配置
- R007（消息持久化）：届时 user 参数将从"仅注入"变为"注入并传递 user_id 到 service 层"

### 6.3 约束

- **不修改函数体**：仅在函数签名新增参数，业务逻辑零改动
- **不修改 service 层**：user 参数不传递到 ChatService / RetrieveService（DEC-auth-006）
- **不修改 health.py**：GET /api/health 不注入 Depends，保持公开（DEC-auth-007）
- **不修改 main.py**：Depends 注入无需注册全局中间件
- **不修改 dependencies.py / schemas.py / errors.py**

## 7. 风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| BF001 未完成导致 import 失败 | Low | 编译错误 | BB001 标记 depends_on BF001，确保先完成 |
| 有效 token 测试中 service 层报错 | Low | 测试误判为鉴权失败 | 使用 `app.dependency_overrides` mock service 层，隔离鉴权与业务 |
| stream_router 集成测试 SSE 读取消复杂 | Medium | 测试编写成本 | 仅验证 status_code + content-type，不解析 SSE 内容 |

## 8. 估算工时

| 步骤 | 工时 |
|------|------|
| Step 1-2: router.py 变更 | 0.05h |
| Step 3-4: stream_router.py 变更 | 0.05h |
| Step 5-6: retrieve.py 变更 | 0.05h |
| Step 7: 7 个集成测试编写 | 0.4h |
| 调试与修复 | 0.15h |
| **合计** | **~0.7h（向上取整 0.8h）** |

## 9. 验证命令

```bash
# 安装依赖（含 BF001 新增的 python-jose）
cd backend && pip install -r requirements.txt

# 运行 BB001 集成测试
pytest tests/test_auth_integration.py -v

# 验证三个路由文件 import 正常
python -c "from app.chat.router import router; print('router OK')"
python -c "from app.chat.stream_router import router; print('stream_router OK')"
python -c "from app.api.routes.retrieve import router; print('retrieve OK')"

# 端到端冒烟（需 BF001 + 部署配置就绪）
# 无 token -> POST /api/chat -> 期望 401
curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:8000/api/chat -H "Content-Type: application/json" -d '{"question":"test","top_k":3}'
# 有效 token -> POST /api/chat -> 期望 200
curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:8000/api/chat -H "Content-Type: application/json" -H "Authorization: Bearer {token}" -d '{"question":"test","top_k":3}'
```
