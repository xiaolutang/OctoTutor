# Sprint Contract: R003-BF-006

## domain
backend

## implementation_plan

### 目标
实现 ChromaDB 封装：定义 VectorStore Protocol 接口（upsert/query/delete），实现 ChromaDBStore（基于 PersistentClient），支持 collection 管理和 metadata where 查询。

### 前置条件
- R003-BF-001 已完成：Monorepo 结构已建立
- R003-BF-002 已完成：FastAPI 后端骨架已搭建
- R003-BF-004 已完成：Chunk, ChunkMetadata 数据模型已定义

### 接口设计

#### VectorStore Protocol
```python
from typing import Protocol, runtime_checkable

@runtime_checkable
class VectorStore(Protocol):
    """向量存储抽象协议

    定义 upsert/query/delete 三个核心操作，
    ChromaDBStore 和未来的 MockStore 都需实现此协议。
    """

    def upsert(self, chunks: list[Chunk], embeddings: list[list[float]]) -> None:
        """批量插入/更新 chunks（含 embedding 向量）

        Args:
            chunks: 分块列表，每个 chunk 包含 chunk_id, text, metadata
            embeddings: 与 chunks 一一对应的 embedding 向量列表
        """
        ...

    def query(
        self,
        query_embedding: list[float],
        top_k: int = 5,
        where: dict | None = None,
    ) -> list[QueryResult]:
        """向量相似度查询

        Args:
            query_embedding: 查询向量
            top_k: 返回最相似的 top-K 结果
            where: ChromaDB where 过滤条件（如 {"book": "必修第一册"}）

        Returns:
            按相似度排序的 QueryResult 列表
        """
        ...

    def delete(self, where: dict) -> None:
        """按 metadata 条件删除

        Args:
            where: ChromaDB where 过滤条件
        """
        ...
```

#### QueryResult 数据类
```python
@dataclass
class QueryResult:
    """查询结果

    Attributes:
        chunk_id: chunk 唯一标识
        text: 分块文本
        metadata: 元数据（ChunkMetadata）
        score: 相似度分数（0-1，越大越相似）
    """
    chunk_id: str
    text: str
    metadata: ChunkMetadata
    score: float
```

#### ChromaDBStore 实现
```python
class ChromaDBStore:
    """基于 ChromaDB PersistentClient 的 VectorStore 实现

    Args:
        persist_directory: 持久化目录路径，默认 "data/chroma_db"
        collection_name: collection 名称，默认 "octotutor_chunks"
    """

    def __init__(
        self,
        persist_directory: str = "data/chroma_db",
        collection_name: str = "octotutor_chunks",
    ):
        ...

    def upsert(self, chunks: list[Chunk], embeddings: list[list[float]]) -> None:
        """实现 Protocol.upsert
        - 校验 chunks 和 embeddings 长度一致
        - 提取 ids, documents, metadatas, embeddings
        - 调用 collection.upsert()
        """
        ...

    def query(
        self,
        query_embedding: list[float],
        top_k: int = 5,
        where: dict | None = None,
    ) -> list[QueryResult]:
        """实现 Protocol.query
        - 调用 collection.query(query_embeddings, n_results, where)
        - 将 ChromaDB 返回结果转换为 QueryResult 列表
        - 计算 score = 1 - distance（cosine distance → similarity）
        """
        ...

    def delete(self, where: dict) -> None:
        """实现 Protocol.delete
        - 调用 collection.delete(where=where)
        """
        ...

    def count(self) -> int:
        """返回 collection 中的文档数量"""
        ...
```

### ChromaDB 配置
- Client: `chromadb.PersistentClient(path=persist_directory)`
- Collection: `client.get_or_create_collection(name=collection_name, metadata={"hnsw:space": "cosine"})`
- 默认持久化路径: `data/chroma_db/`
- 默认 collection 名: `octotutor_chunks`
- 距离度量: cosine similarity

### Metadata Where 查询支持
支持以下过滤条件（直接透传 ChromaDB where 语法）：
- `{"book": "必修第一册"}` - 精确匹配书名
- `{"page": 12}` - 精确匹配页码
- `{"chunk_type": "child"}` - 精确匹配类型
- `{"has_formula": True}` - 精确匹配布尔值
- `{"$and": [{"book": "必修第一册"}, {"chunk_type": "child"}]}` - 组合条件
- `{"page": {"$gte": 10, "$lte": 20}}` - 范围查询

### ChromaDB metadata 类型映射
ChromaDB metadata 支持的类型: string, int, float, bool。
ChunkMetadata.to_dict() 的输出可直接作为 ChromaDB metadata 使用。

### score 计算
ChromaDB 使用 cosine distance (0~2)，转换为 similarity score:
```python
score = 1 - distance  # distance=0 → score=1（完全相同）
```

### 目标文件结构
```
services/backend/
├── app/rag/
│   ├── __init__.py           # 已存在
│   ├── models.py             # 已存在，新增 QueryResult
│   ├── chunkers/             # 已存在
│   └── vector_store.py       # 新增: VectorStore Protocol + ChromaDBStore
└── tests/
    └── test_vector_store.py  # 新增: 单元测试
```

### 变更步骤

#### Step 1: 修改 `app/rag/models.py`
新增 QueryResult 数据类

#### Step 2: 创建 `app/rag/vector_store.py`
实现 VectorStore Protocol 和 ChromaDBStore 类

#### Step 3: 创建 `tests/test_vector_store.py`
编写单元测试，使用临时目录避免污染生产数据

#### Step 4: 运行 pytest 验证
确保所有测试通过

## acceptance_criteria_negotiated
- [ ] `services/backend/app/rag/vector_store.py` 存在，定义 VectorStore Protocol 和 ChromaDBStore 类
- [ ] `services/backend/app/rag/models.py` 新增 QueryResult 数据类
- [ ] VectorStore Protocol 定义 upsert/query/delete 三个方法签名
- [ ] ChromaDBStore 使用 PersistentClient 初始化，数据持久化到 data/chroma_db/
- [ ] collection 名称: octotutor_chunks，距离度量: cosine
- [ ] upsert: 批量插入 chunks（含 embedding + metadata），校验 chunks/embeddings 长度一致
- [ ] query: 输入 embedding + top_k + where → 返回 QueryResult[]（按相似度降序）
- [ ] delete: 按 where 条件删除匹配的文档
- [ ] count: 返回 collection 中文档数量
- [ ] score = 1 - distance（cosine distance 转换为 similarity）
- [ ] metadata where 过滤（book, page, chunk_type, has_formula）正确
- [ ] upsert chunks → query → 返回正确结果
- [ ] delete 后 query 不再返回已删除数据
- [ ] PersistentClient 数据持久化（重启客户端后数据仍在）
- [ ] pytest 测试全部通过

## boundary

### 本任务范围（IN）
- VectorStore Protocol 接口定义
- QueryResult 数据类
- ChromaDBStore 实现（upsert/query/delete/count）
- metadata where 查询支持
- collection 管理（get_or_create）
- 单元测试（使用临时目录）

### 不在本任务范围（OUT）
- Embedding 向量化（R003-BF-005）
- 入库编排（R003-BF-007）
- 检索 API（R003-BB-008）
- PDF 读取和 OCR（R003-BF-003）
- 数据导入脚本
- 前端界面

### 禁止修改
- `services/frontend/` 下任何文件
- `deploy/` 下任何文件
- `.dev-flow/` 下除 contracts/R003-BF-006.md 和 evidence/R003-BF-006.md 外的文件
- `services/backend/app/config.py`
- `services/backend/app/main.py`
- `services/backend/app/api/` 下任何文件
- `services/backend/app/rag/chunkers/` 下任何文件

### 允许修改
- `services/backend/app/rag/models.py`（新增 QueryResult）
- `services/backend/app/rag/vector_store.py`（新建）
- `services/backend/tests/test_vector_store.py`（新建）

## risk_negotiation

| 风险标签 | 风险描述 | 缓解措施 |
|---------|---------|---------|
| chromadb_api | ChromaDB API 可能有版本差异 | 使用 chromadb>=0.5.0 稳定 API；upsert 而非 add 保证幂等 |
| persistent_client | PersistentClient 在并发场景下的行为 | OctoTutor 为单进程嵌入式使用，无并发问题 |
| distance_metric | cosine distance 与 similarity 转换 | score = 1 - distance，并在测试中验证转换正确性 |
| test_isolation | 测试不能污染生产数据 | 使用 tmp_path fixture 创建临时目录，测试后自动清理 |

## contract_alignment

### 与分析文档对齐
- Metadata 字段完全对齐 models.py 中的 ChunkMetadata.to_dict()
- ChromaDB 使用方式（PersistentClient + cosine）完全遵循 architecture.md
- collection 名称 octotutor_chunks 与 api_contracts.md 对齐
- 持久化路径 data/chroma_db/ 与架构决策对齐

### 与后续任务的接口
- ChromaDBStore 可被入库编排（R003-BF-007）直接使用
- query 方法返回 QueryResult[]，可被检索 API（R003-BB-008）映射为 HTTP 响应
- VectorStore Protocol 使未来可替换为其他向量存储实现
- count 方法可被 health endpoint 使用
