# Evidence: R003-BF-012

## 任务信息
- task_id: R003-BF-012
- name: Docker Compose 集成 + 迁移验证
- module: docker-integration
- domain: infra

## 变更文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `deploy/docker-compose.yml` | 修改 | 新增 backend 服务 + 重命名 frontend 服务 + Traefik 路由 + 资源限制 |
| `deploy/docker-compose.local.yml` | 修改 | 新增 backend 服务 + 重命名 frontend 服务 + 本地路由 |
| `deploy/build.sh` | 修改 | 新增后端镜像构建 + 支持 --frontend-only/--backend-only |
| `deploy/deploy.sh` | 修改 | 更新健康检查（前端 + 后端）+ 支持更多选项 |
| `.dev-flow/contracts/R003-BF-012.md` | 新增 | Sprint Contract |

## 验证结果

### 1. docker compose config 验证

**生产配置** (`docker-compose.yml`):
```
$ docker compose -f deploy/docker-compose.yml config --quiet
配置有效（warning 为环境变量未设置，属正常）
```

验证要点：
- octotutor-frontend 服务存在，image: octotutor-frontend:latest
- octotutor-backend 服务存在，image: octotutor-backend:latest
- 后端 Traefik 路由：Host + PathPrefix(`/api/`)，priority=10
- 前端 Traefik 路由：Host catch-all，priority=1
- 后端 healthcheck 配置正确（/api/health）
- octotutor-data named volume 持久化
- 后端 memory limit: 1024M，前端 memory limit: 512M
- 网络配置正确（gateway + auth-network）

**本地配置** (`docker-compose.local.yml`):
```
$ docker compose -f deploy/docker-compose.local.yml config --quiet
配置有效
```

验证要点：
- 前后端双服务配置正确
- 后端 DASHSCOPE_API_KEY 默认值 sk-dummy-for-local
- auth-network 映射到 auth-network-local
- octotutor-data-local volume

### 2. Shell 脚本语法验证

```
$ bash -n deploy/build.sh → 语法正确
$ bash -n deploy/deploy.sh → 语法正确
```

### 3. Dockerfile 验证

后端 Dockerfile (`services/backend/Dockerfile`) 已存在且结构正确：
- 基于 python:3.12-slim
- 安装 requirements.txt
- 暴露 8000 端口
- 启动 uvicorn app.main:app

### 4. Traefik 路由规则验证

| 环境 | 路由 | 规则 | 优先级 | 目标 |
|------|------|------|--------|------|
| 生产 | octotutor-frontend | Host(octotutor.xiaolutang.top) | 1 | :3000 |
| 生产 | octotutor-backend | Host + PathPrefix(/api/) | 10 | :8000 |
| 本地 | octotutor-frontend-local | Host(octotutor.localhost) | 1 | :3000 |
| 本地 | octotutor-backend-local | Host + PathPrefix(/api/) | 10 | :8000 |

路由优先级：后端(10) > 前端(1)，确保 /api/* 请求路由到 FastAPI。

### 5. 验收标准逐项检查

- [x] `deploy/docker-compose.yml` 包含 octotutor-frontend 和 octotutor-backend 两个服务
- [x] `deploy/docker-compose.local.yml` 包含 octotutor-frontend 和 octotutor-backend 两个服务
- [x] 后端服务使用 services/backend/Dockerfile 构建（build.sh 中指定）
- [x] 后端 Traefik 路由：PathPrefix(/api/) 优先级高于前端 catch-all
- [x] 前端 Traefik 路由：catch-all（低优先级）
- [x] 后端 health check 配置正确（/api/health）
- [x] 后端环境变量可配置（DASHSCOPE_API_KEY 等）
- [x] 后端数据目录有 volume 持久化（octotutor-data）
- [x] docker compose config 命令不报错
- [x] build.sh 支持构建后端镜像（+ --frontend-only/--backend-only）
- [x] 服务器资源约束：后端 1GB、前端 512MB memory limit
- [x] 前端功能不变（未修改前端代码）

### 6. 未实际执行 docker compose up

按照任务要求，未实际执行 `docker compose up`，避免端口冲突。
仅通过 `docker compose config` 验证配置正确性。

## 资源分析

服务器 4 核 4GB 资源分配预估：
- Traefik: ~50MB
- octotutor-frontend: limit 512MB，实际约 100-200MB
- octotutor-backend: limit 1024MB，实际约 200-400MB（ChromaDB 嵌入式）
- 系统 + 其他: ~500MB
- **合计约 850-1150MB，在 4GB 约束内安全运行**
